#from "math.in" import sin, cos

const a := 3

const add := fun(a,b) return a+b end

fun foo(a,b)
  m = [[0 for k in 0 to 100] for p in 0 to 100]
  atomic b := 6*(6+2)
  b += 2
  c = fun(x) print(x) end
  v = [1,"hello",2]
  if b == 8
    print(c,"str",sin(3*pi))
  end
  f = { hello : 3 }
  f["hello"] = fish
  atomic i := 0
  while i < 3
    i += 1
  end
end

fun main(arg)
    print("Hello, world")
end
